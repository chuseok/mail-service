spring:
  application:
    name: mail-service

  datasource:
    url: jdbc:postgresql://localhost:5432/mail_service
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
#      테이블 자동 반영
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: cecange11111@gmail.com
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
